<%= form_for(@subnet) do |f| %>
  <% if @subnet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subnet.errors.count, "error") %> prohibited this subnet from being saved:</h2>

      <ul>
      <% @subnet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :network %><br />
    <%= f.text_field :network %>
  </div>
  <div class="field">
    <%= f.label :firstaddr %><br />
    <%= f.text_field :firstaddr %>
  </div>
  <div class="field">
    <%= f.label :bcastaddr %><br />
    <%= f.text_field :bcastaddr %>
  </div>
  <div class="field">
    <%= f.label :mask %><br />
    <%= f.text_field :mask %>
  </div>
  <div class="field">
    <%= f.label :system %><br />
    <%= collection_select(:subnet, :system_id, System.all, :id, :name, :prompt => true) %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
